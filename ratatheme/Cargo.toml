[package]
name = "ratatheme"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatheme-types = { version = "0.1.0", path = "../ratatheme-types" }
ratatheme-derive = { version = "0.1.0", path = "../ratatheme-derive" }

# BOOM: Dev dependency
ratatui = { version = "0.29.0" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
